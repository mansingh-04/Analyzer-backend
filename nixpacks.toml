[phases.setup]
aptPkgs = ["python3", "python3-pip", "python3-venv", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"

[variables]
PORT = "5050" 